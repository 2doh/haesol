import { atom } from "recoil";

export const userNameState = atom({ key: "userNameState", default: "" });
export const userClassState = atom({ key: "userClassState", default: "" });
export const userRoleState = atom({ key: "userRoleState", default: "" });
